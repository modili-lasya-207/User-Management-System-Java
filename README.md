# User-Management-System-Java
Console-based User Management System in Java implementing CRUD operations. Features: Add, View, Search, Update, Delete users. Uses OOP concepts, ArrayList for dynamic storage, Iterator for safe deletion, and Scanner for interactive menu-driven input.
